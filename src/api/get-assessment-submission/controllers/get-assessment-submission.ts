/**
 * get-assessment-submission controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::get-assessment-submission.get-assessment-submission');
